# HSGalaxy v0.1.0 — feat/vortice-dcomp → main

This PR finalizes Phase 5.4 (Wizard Offline/429 + banner), Phase 4.1 (DPI cross-checks), and Phase 1.2 (long-path runtime), adds Wizard UX polish, testing, and packaging utilities.

## Highlights
- Wizard Offline/429 fallback: visual banner + overlay.log proof
- DPI: strip renders at 120/144/200 DPI; Per-Monitor-V2 awareness
- Long-path runtime proof (len≈401) and storage validations
- Wizard UX: Rename… and Delete… with confirmations + live refresh
- Evidence tooling: CLI `evidence:bundle` (ZIP) and `evidence:report` (HTML)
- Unit tests: Core (4) and OCR (3) PASS on .NET 8
- Publish script: App/CLI zipped artifacts for win-x64

## Evidence Pointers
- overlay.log: D:\\cursor_bots\\HSGalaxy\\logs\\overlay.log (timestamps in checklist)
- Screenshots: %TEMP%\\HSGalaxy\\wizard_selftest_offline_*.png
- Strips: %TEMP%\\HSGalaxy\\strip_*.png
- Evidence ZIP: see checklist entry for full path
- Evidence HTML: %TEMP%\\HSGalaxy\\report_evidence_YYYYMMDD_HHMMSS.html

## Commands
- App (Wizard only): `HSGALAXY_DISABLE_OVERLAY=1 HSGALAXY_SHOW_WIZARD=1 dotnet run --project src/HSGalaxy.App`
- OCR offline: set Azure env + `HSGALAXY_OCR_FORCE_OFFLINE=1` → `ocr:test`
- OCR 429: set Azure env + `HSGALAXY_OCR_FORCE_429=1` → `ocr:test`
- DPI strips: `strip:render 120/144/200 dark|light|safe`
- Evidence: `evidence:bundle`, `evidence:report`
- Publish: `tools\\Scripts\\publish.ps1`

## Notes / Warnings
- CA1416 platform warnings are expected (Windows-only APIs) and non-blocking.
- No secrets in repo; Azure keys are via env vars during tests.

## Version Tag
- `v0.1.0` created and pushed.

## Next
- Optional: GitHub Release attaching artifacts and evidence ZIP
- Optional: Extend tests for Wizard rename/delete flows using UI automation harness
